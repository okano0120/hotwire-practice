<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <%= turbo_frame_tag product, src: product_path(product) %>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>
